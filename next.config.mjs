/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['https://mqtcodpajykyvodmahlt.supabase.co'], // Replace with your actual Supabase storage domain
  },
};

export default nextConfig;
